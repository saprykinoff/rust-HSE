#![forbid(unsafe_code)]
use std::thread::Thread;

pub fn sequential_run<Printer, Iter>(
    printer: Printer,
    iterator: Iter,
    odd_thread_ms: u64,
    even_thread_ms: u64,
    max_iterations: usize,
)
{
    // TODO: your code goes here.
    unimplemented!()
}

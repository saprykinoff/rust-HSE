#![forbid(unsafe_code)]

// TODO: your code goes here.
unimplemented!()
struct FlipCoinConfig {
    pub players_proba: (f64, f64),
    pub players: (u8, u8),
    pub birthday_player: u8,
    pub delta: f64,
}
